export enum ActionType {
  Initialize = 'Initialize',
  DealCards = 'DealCards',
  SetCards = 'SetCards',
  HoldCard = 'HoldCard',
  Deal = 'Deal',
  Draw = 'Draw',
  SetHeaderData = 'SetHeaderData',
}
