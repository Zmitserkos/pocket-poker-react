import React from 'react';

interface PokerCardProps {
  card: any;
}

const PokerCard: React.FC<PokerCardProps> = (props) => {
  return (
    <div className="poker-card">
    </div>
  );
};

export default PokerCard;
